---
name: qa-checklist
description: Nine Chronicles 밸런스 시트를 CSV로 내보낸 전/후 두 버전을 키 컬럼(보통 Id) 기준으로 대조해, 추가/삭제/변경된 행과 컬럼을 QA 체크리스트 형태로 뽑을 때 사용. "이번 패치 시트 diff로 QA 체크리스트 만들어줘", "이 두 CSV 뭐가 달라졌는지 체크리스트로 뽑아줘" 같은 요청에 사용. 로컬 CSV 파일만 읽으므로 권한 승인 없이 바로 실행 가능. ⚠️ 원래 설계 문서가 그리던 "시트 diff + 기능 범위 → QA 체크리스트" 중 이 스킬은 시트 diff(무엇이 바뀌었는지)만 만든다 — "그래서 무엇을 테스트해야 하는지"(시트별 기능 매핑)는 lib9c 도메인 지식이 필요해 범위 밖이다(아래 참고).
---

# qa-checklist (부분 구현 — 시트 diff만, 기능 매핑은 사람 몫)

> 이 스킬은 2026-09-01 세션에서 처음 SKILL.md로 작성됐다. 설계 문서("나인 크로니클 업데이트
> 자동화 설계" §3 6단계)가 qa-checklist에 요구한 것 중 **스키마 지식 없이 바로 만들 수 있는
> 절반만** 구현했다. 왜 이렇게 됐는지 먼저 읽을 것.

## 무엇이 왜 이렇게 됐는지

설계 문서는 이 스킬의 입력을 "시트 diff + 기능 범위", 출력을 "회귀 테스트 체크리스트·케이스
이슈"로 그린다. 실제로 만들어보니 성격이 갈렸다:

- **시트 diff** — 전/후 CSV를 키 컬럼 기준으로 대조해 어떤 행이 추가/삭제됐고, 어떤 행의
  어떤 컬럼 값이 바뀌었는지 계산하는 건 시트가 무슨 스키마인지 몰라도 할 수 있다. **이번에
  만든 것.** `datasheet-validate`의 RFC4180 CSV 파서(`tools/9c/lib/datasheet-validate.ts`의
  `parseCsv`)를 그대로 재사용한다.
- **"이 시트가 바뀌면 어떤 기능을 테스트해야 하는지"** — 예를 들어 `SkillSheet`이 바뀌면
  스킬 데미지를, `MaterialItemSheet`이 바뀌면 제작을 테스트해야 한다는 매핑은 시트마다 다른
  lib9c 도메인 지식이 필요하다. `datasheet-validate`가 "시트 간 참조 ID 검증"을 스키마
  매핑이 먼저 필요하다는 이유로 미룬 것과 정확히 같은 이유로 **미착수.**

그래서 이 스킬이 실제로 내는 체크리스트는 "이 시트의 Id 1234번, 컬럼 Damage가 100→120으로
바뀌었다"는 **사실 목록**이다. "그러니 스킬 데미지를 실전에서 확인하라"는 QA 담당자가
판단해서 채운다 — 이 스킬은 어디를 봐야 하는지 정확히, 빠짐없이 짚어주는 것까지만 한다.

## 도구 현황

| 도구 | 위치 | 역할 |
| --- | --- | --- |
| `qa-checklist.ts` | `tools/9c/qa-checklist.ts` (bun) | CLI 본체 |
| diff/체크리스트 로직 | `tools/9c/lib/qa-checklist.ts` | 순수 함수(유닛 테스트 대상) — `datasheet-validate.ts`의 `parseCsv` 재사용 |
| 유닛 테스트 | `tools/9c/lib/qa-checklist.test.ts` | diff 분류·체크리스트 문구·중복 키 처리, 네트워크 없이 실행 |

실행: `bun test tools/9c/lib/qa-checklist.test.ts`. 네트워크를 안 쓰는 순수 로컬 도구라 별도
라이브 검증기는 없다(datasheet-validate/release-guard와 달리 인증·공개 엔드포인트 의존이
없음).

## 1. 무엇을 하는가

두 CSV(패치 전/후, 같은 시트)를 `--key-column`(보통 `Id`) 기준으로 대조한다.

1. **컬럼 추가/삭제** — 헤더 집합이 달라졌으면 알려준다(순서 변경은 무시).
2. **행 추가/삭제** — 키가 한쪽에만 있는 행을 골라낸다.
3. **행 변경** — 양쪽에 공통으로 존재하는 컬럼만 비교해서, 값이 다른 컬럼과 전/후 값을
   `Id=1234: Damage "100"→"120"` 형태로 나열한다. 양쪽에만 있는 컬럼(위 1번)은 여기서
   비교하지 않는다 — 그건 이미 별도 항목으로 보고됐다.
4. **중복 키 경고** — 같은 파일 안에 같은 키가 두 번 이상 있으면 `[!]`로 별도 표시한다.
   diff 계산은 마지막 값만 반영하므로, 이 경고가 뜨면 원본을 먼저 확인해야 한다.
5. **변경 없음도 명시** — 아무 차이가 없으면 빈 목록이 아니라 "변경 없음"을 명시적으로
   출력한다(빈 출력을 "확인 안 함"과 헷갈리지 않도록).

## 2. 실행

```bash
bun run tools/9c/qa-checklist.ts \
  --sheet-name MaterialItemSheet \
  --before ./MaterialItemSheet.old.csv \
  --after ./MaterialItemSheet.new.csv \
  --key-column Id

# JSON으로 (전체 변경 목록 확인용 — 사람이 읽는 출력은 20건 넘으면 샘플링됨)
bun run tools/9c/qa-checklist.ts --sheet-name MaterialItemSheet --before old.csv --after new.csv --key-column Id --json
```

시트 탭(=CSV 파일)마다 따로 실행한다 — `datasheet-validate`와 동일하게 한 번에 한 쌍만
본다. 두 CSV를 준비하는 방법(구글 시트 gviz export URL 등)은 `datasheet-validate` SKILL.md
참고.

## 3. 범위 밖 (설계 문서 대비 의도적으로 뺀 것)

- **시트별 기능 매핑** ("이 컬럼이 바뀌면 이 기능을 테스트하라") — lib9c 도메인 지식 필요.
  미착수. `datasheet-validate`의 "시트 간 참조 ID 검증"과 같은 선행 조사가 필요하다.
- **실제 QA 실행·케이스 이슈 등록** — 이 스킬은 텍스트 체크리스트만 낸다. 이슈 트래커에
  올리는 건 사람이 한다(D4 원칙과 무관하게, 애초에 이 스킬이 다루는 범위가 아님).
- **회차 간 자동 기준선 선택** (N-1을 어디서 가져올지) — `datasheet-validate`의 미해결 B와
  같은 문제. 이 스킬은 `--before`/`--after`를 사람이 직접 지정하게 해서 이 문제를 우회한다
  — 무엇을 "이전"으로 볼지는 항상 사람이 정한다.
- **타입 인식 diff** (숫자 컬럼의 증감 방향, 퍼센트 변화 등) — 값은 항상 문자열로 비교한다.
  `"100"`과 `"100.0"`은 다른 값으로 잡힌다 — 시트마다 포맷 관례가 다를 수 있어 임의로
  정규화하지 않는다.

## 4. 다음에 할 일

| 항목 | 상태 | 필요한 것 |
| --- | --- | --- |
| 시트별 기능 매핑 | 미착수 | lib9c 시트→기능 매핑 (별도 조사, `datasheet-validate`의 참조 ID 검증과 공유 가능) |
| 숫자/타입 인식 비교 | 미착수 | 시트별 컬럼 타입 정의 (별도 조사) |
| 회차 간 자동 기준선 | 미착수 | 비교 기준선 저장 방식 결정(미해결 B) |
